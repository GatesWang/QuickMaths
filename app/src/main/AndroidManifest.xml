<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.wang.gates.quickmaths">

    <application
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/AppTheme.NoActionBar">
        <activity android:name=".activities.GameScore">
        </activity>
        <activity android:name=".activities.modes.GameLocalPlayer"/>
        <activity android:name=".activities.modes.GameMultiplayer"/>
        <activity android:name=".activities.modes.GameSinglePlayer"/>
        <activity android:name=".activities.Game"/>
        <activity
                android:name=".activities.MainMenu"
                android:label="@string/title_activity_main">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".activities.Settings"/>
    </application>

</manifest>