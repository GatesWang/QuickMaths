package com.wang.gates.quickmaths.custom_views

import android.content.Context
import android.util.AttributeSet
import com.wang.gates.quickmaths.R

class DrawViewAnswer(context: Context, attrs: AttributeSet) : DrawView(context, attrs) {

    init{
        setBackgroundResource(R.drawable.rectangle_border)
    }
}